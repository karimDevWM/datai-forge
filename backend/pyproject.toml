[project]
name = "backend"
version = "0.1.0"
description = "ETL and ML backend for Lyon presidential election analysis"
requires-python = ">=3.12"
dependencies = [
    "pyspark==3.5.0",
    "pandas==2.2.0",
    "pyarrow==15.0.0",
    "matplotlib==3.8.3",
    "seaborn==0.13.2",
    "statsmodels==0.14.1",
    "scikit-learn==1.4.2",
    "jinja2>=3.1.0",
    "setuptools",
    "ipykernel",
    "nbstripout==0.8.1",
    "pytest==8.3.4",
    "pytest-cov==4.1.0",
    "pre-commit==4.1.0",
    "ruff==0.3.4",
    "bandit==1.7.8",
    "safety==3.1.0",
    "mysql-connector-python==9.6.0",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W", "B", "I", "UP"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "--cov=src --cov-report=term-missing -v"
